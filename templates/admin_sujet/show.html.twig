{% extends 'base.html.twig' %}

{% block title %}Sujet{% endblock %}

{% block body %}
    <h1>Sujet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sujet.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ sujet.nom }}</td>
            </tr>
            <tr>
                <th>Texte</th>
                <td>{{ sujet.texte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_sujet_index') }}">back to list</a>

    <a href="{{ path('app_admin_sujet_edit', {'id': sujet.id}) }}">edit</a>

    {{ include('admin_sujet/_delete_form.html.twig') }}
{% endblock %}
