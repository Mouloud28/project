{% extends 'base.html.twig' %}

{% block title %}Artiste index{% endblock %}

{% block body %}
    <h1>Artiste index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Date_naissance</th>
                <th>Pays_origine</th>
                <th>Photo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artiste in artistes %}
            <tr>
                <td>{{ artiste.id }}</td>
                <td>{{ artiste.nom }}</td>
                <td>{{ artiste.dateNaissance ? artiste.dateNaissance|date('Y-m-d') : '' }}</td>
                <td>{{ artiste.paysOrigine }}</td>
                <td><img src="{{ vich_uploader_asset(artiste, 'imageFile') }}" alt="Hayao Miyazaki"></td>
                <td>
                    <a href="{{ path('app_admin_artiste_show', {'id': artiste.id}) }}">show</a>
                    <a href="{{ path('app_admin_artiste_edit', {'id': artiste.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_artiste_new') }}">Create new</a>
{% endblock %}
