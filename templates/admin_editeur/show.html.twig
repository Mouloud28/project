{% extends 'base.html.twig' %}

{% block title %}Editeur{% endblock %}

{% block body %}
    <h1>Editeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editeur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ editeur.nom }}</td>
            </tr>
            <tr>
                <th>Pays_origine</th>
                <td>{{ editeur.paysOrigine }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ editeur.dateCreation ? editeur.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_editeur_index') }}">back to list</a>

    <a href="{{ path('app_admin_editeur_edit', {'id': editeur.id}) }}">edit</a>

    {{ include('admin_editeur/_delete_form.html.twig') }}
{% endblock %}
