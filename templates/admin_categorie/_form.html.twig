{{ form_start(form) }}
    {{ form_widget(form) }}
    
<button class="btn-ajouter-categorie form-control mx-5 my-3 btn fw-bold">{{ button_label|default('Ajouter la catégorie') }}</button>

{{ form_end(form) }}
