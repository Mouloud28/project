<form method="post" action="{{ path('app_role_artiste_film_delete', {'id': role_artiste_film.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role_artiste_film.id) }}">
    <button class="btn">Delete</button>
</form>