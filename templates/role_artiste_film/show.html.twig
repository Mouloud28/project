{% extends 'base.html.twig' %}

{% block title %}RoleArtisteFilm{% endblock %}

{% block body %}
    <h1>RoleArtisteFilm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role_artiste_film.id }}</td>
            </tr>
            <tr>
                <th>Role</th>
                {# <td>{{ role_artiste_film.role }}</td> #}
                <td>

                    {% if role_artiste_film.role %}
						{% for role in role_artiste_film.role %}
						{{ role.nom }}{% endfor %}
					{% else %}
						<span class="text-danger fst-italic">N.R</span>
					{% endif %}
                    </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_role_artiste_film_index') }}">back to list</a>

    <a href="{{ path('app_role_artiste_film_edit', {'id': role_artiste_film.id}) }}">edit</a>

    {{ include('role_artiste_film/_delete_form.html.twig') }}
{% endblock %}
